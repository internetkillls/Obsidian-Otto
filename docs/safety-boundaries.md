# Safety Boundaries

## Local-first
All raw vault processing is local by default.

## Wellbeing and SWOT
These are opt-in and local-only.
By default Otto only uses explicit frontmatter fields, not free-text emotional inference.

## No automatic training
Training exports are created locally.
Actual fine-tuning is manual and gated by data quality.
