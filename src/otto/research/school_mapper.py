from __future__ import annotations


def map_school(topic: str) -> dict[str, str]:
    return {"topic": topic, "entry_mode": "onboarding_before_critique", "default_question": "Who lives inside this problem?"}
